spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:enqueteResultado
    username: sa
    password:
  jpa:
    properties:
      hibernate.format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2Console      
